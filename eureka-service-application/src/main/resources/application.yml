server:
  port: 8090

eureka:
  client:
     register-with-eureka: false
     fetch-registry: false
     serviceUrl:
       defaultZone: http://localhost:8090/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/ajaysh540/java-task-config-server.git'